
<project xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.nanocontainer</groupId>
        <artifactId>nanocontainer-nanosar-parent</artifactId>
        <version>2.0-SNAPSHOT</version>
    </parent>
    <artifactId>nanocontainer-nanosar-example</artifactId>
    <name>NanoContainer deployer MBean example</name>
    <packaging>jar</packaging>
    <dependencies>
        <dependency>
            <groupId>org.nanocontainer</groupId>
            <artifactId>nanocontainer-nanosar</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>org.nanocontainer</groupId>
            <artifactId>nanocontainer-groovy</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>com.thoughtworks.xstream</groupId>
            <artifactId>xstream</artifactId>
            <version>1.2.1</version>
        </dependency>
    </dependencies>
    <build>
        <resources>
            <resource>
                <directory>src/resources</directory>
                <includes>
                    <include>**/*.groovy</include>
                    <include>**/*.xml</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>jboss-packaging-maven-plugin</artifactId>
                <version>2.0-SNAPSHOT</version>
                <configuration>
                    <deploymentDescriptorFile>${basedir}/src/resources/META-INF/jboss-service.xml</deploymentDescriptorFile>
                </configuration>
                <executions>
                    <execution>
                        <id>sar</id>
                        <goals>
                            <goal>sar</goal>
                        </goals>
                        <phase>package</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>deploy sar</id>
                        <phase>integration-test</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <tasks>
                                <delete file="${jboss.deploy.dir}/${pom.artifactId}-${pom.version}.sar"/>
                                <copy
                                        file="${project.build.directory}/${pom.artifactId}-${pom.version}.sar"
                                        todir="${jboss.deploy.dir}"
                                        overwrite="true" />

                            </tasks>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>